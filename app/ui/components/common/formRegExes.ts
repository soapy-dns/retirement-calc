export const INTEGERS_ONLY = /^[0-9]+$/
export const DECIMALS_ONLY = /^[0-9]*\.?[0-9]*$/
export const ALPHA_ONLY = /^[A-Za-z_\s]+$/ // TODO: should probably restrict better
export const ALPHA_NUMERIC = /^[A-Za-z_0-9\s]+$/
